#include<stdio.h>
#include<conio.h>
void main()
{
char h;
printf("enter the character");
scanf("%s",&h);
if((h>=a && h<=z)||(h>>=A && h<=Z))
{
printf("character is alphabet");
}
else
{
printf("not a alphabet");
}
}

