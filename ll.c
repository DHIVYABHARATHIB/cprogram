#include<stdio.h>
#include<conio.h>
void  main()
{
int i,num,sum arr[50];
  printf("enter the number");
  scanf("%d",&num);
  printf("enter the values");
  for(i=0;i<num;i++)
  scanf("%d",&arr[i]);
  sum=0;
  for(i=0;i<num;i++)
    printf("enter the array");
  printf("sum is");
}
