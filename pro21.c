#include<stdio.h>
const float n = 0.5;
int main(void)
{
      float a;

       printf("Please enter a value to be rounded: ");
       scanf("%f", &n);

       n = n + add;
       n = (int)n;

       printf("The rounded value is %.2f", n);

      return 0;
}
